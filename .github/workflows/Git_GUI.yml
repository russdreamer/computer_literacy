name: CI

on:
  push:
    branches: [ git_gui ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@master
      
    - name: check homework "Git GUI"
      shell: bash
      run: | 
        chmod +x scripts/homework.sh
        scripts/homework.sh
